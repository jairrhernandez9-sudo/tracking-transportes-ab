<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Etiqueta de Envío - <%= envio.numero_tracking %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Arial', 'Helvetica', sans-serif;
      background: #e5e5e5;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }
    
    /* Contenedor de la etiqueta - 4x6 pulgadas */
    .label-container {
      width: 4in;
      height: 6in;
      background: white;
      border: 3px solid #000;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
      position: relative;
      overflow: hidden;
    }
    
    /* Grid principal */
    .label-grid {
      display: grid;
      grid-template-rows: auto auto auto auto auto auto;
      height: 100%;
    }
    
    /* Header - Logo + To Address */
    .header-section {
      display: grid;
      grid-template-columns: 120px 1fr;
      border-bottom: 3px solid #000;
    }
    
    .logo-box {
      border-right: 3px solid #000;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 15px;
      background: white;
    }
    
    .logo-text {
      font-size: 36px;
      font-weight: 900;
      line-height: 0.9;
      text-align: center;
      letter-spacing: -2px;
    }
    
    .to-address-box {
      padding: 12px 15px;
    }
    
    .to-label {
      font-size: 11px;
      font-weight: 700;
      margin-bottom: 4px;
    }
    
    .to-company {
      font-size: 16px;
      font-weight: 900;
      margin-bottom: 2px;
      text-transform: uppercase;
    }
    
    .to-details {
      font-size: 12px;
      line-height: 1.3;
      font-weight: 600;
    }
    
    /* Priority Mail */
    .priority-section {
      background: white;
      border-bottom: 3px solid #000;
      padding: 10px 15px;
      text-align: center;
    }
    
    .priority-text {
      font-size: 24px;
      font-weight: 900;
      letter-spacing: 2px;
    }
    
    /* From Section */
    .from-section {
      border-bottom: 3px solid #000;
      padding: 12px 15px;
    }
    
    .from-label {
      font-size: 11px;
      font-weight: 700;
      margin-bottom: 4px;
    }
    
    .from-company {
      font-size: 14px;
      font-weight: 900;
      margin-bottom: 2px;
    }
    
    .from-details {
      font-size: 11px;
      line-height: 1.3;
      font-weight: 600;
    }
    
    /* Order Info Grid */
    .order-info-section {
      display: grid;
      grid-template-columns: 1fr 1fr;
      border-bottom: 3px solid #000;
    }
    
    .order-left {
      border-right: 3px solid #000;
      padding: 12px 15px;
    }
    
    .order-right {
      padding: 12px 15px;
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    
    .order-label {
      font-size: 10px;
      font-weight: 900;
      margin-bottom: 6px;
      letter-spacing: 0.5px;
    }
    
    .order-value {
      font-size: 14px;
      font-weight: 900;
      font-family: 'Courier New', monospace;
    }
    
    .barcode-small {
      margin-top: 5px;
    }
    
    .barcode-small canvas {
      max-width: 100%;
      height: auto;
    }
    
    .info-field {
      border-bottom: 2px solid #000;
      padding-bottom: 8px;
    }
    
    .info-field:last-child {
      border-bottom: none;
    }
    
    /* Item Info Grid */
    .item-info-section {
      display: grid;
      grid-template-columns: 1fr 1fr;
      border-bottom: 3px solid #000;
    }
    
    .item-left {
      border-right: 3px solid #000;
      padding: 12px 15px;
    }
    
    .item-right {
      padding: 12px 15px;
    }
    
    /* Footer Section */
    .footer-section {
      display: grid;
      grid-template-columns: auto auto 1fr;
      align-items: center;
      padding: 15px;
      gap: 20px;
    }
    
    .fragile-icon {
      text-align: center;
    }
    
    .fragile-icon svg {
      width: 40px;
      height: 50px;
    }
    
    .fragile-text {
      font-size: 10px;
      font-weight: 900;
      margin-top: 3px;
    }
    
    .arrows-icon {
      text-align: center;
    }
    
    .arrow {
      font-size: 50px;
      font-weight: 900;
      line-height: 0.7;
    }
    
    .arrow-line {
      height: 8px;
      background: #000;
      margin-top: -5px;
    }
    
    .barcode-large {
      text-align: center;
    }
    
    .barcode-large canvas {
      max-width: 100%;
      height: auto;
    }
    
    .barcode-number {
      font-size: 12px;
      font-weight: 900;
      font-family: 'Courier New', monospace;
      margin-top: 3px;
    }
    
    /* Botones de impresión */
    .print-buttons {
      position: fixed;
      top: 20px;
      right: 20px;
      display: flex;
      gap: 10px;
      z-index: 1000;
    }
    
    .btn-print {
      padding: 12px 24px;
      background: #1e40af;
      color: white;
      border: none;
      border-radius: 8px;
      font-weight: 700;
      font-size: 14px;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
      box-shadow: 0 4px 12px rgba(30, 64, 175, 0.3);
      transition: all 0.3s ease;
    }
    
    .btn-print:hover {
      background: #1e3a8a;
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(30, 64, 175, 0.4);
    }
    
    .btn-secondary {
      background: #6b7280;
    }
    
    .btn-secondary:hover {
      background: #4b5563;
    }
    
    /* Estilos de impresión */
    @media print {
      body {
        background: white;
        padding: 0;
        margin: 0;
      }
      
      .label-container {
        width: 4in;
        height: 6in;
        box-shadow: none;
        margin: 0;
        page-break-after: always;
      }
      
      .print-buttons {
        display: none;
      }
    }
    
    @page {
      size: 4in 6in;
      margin: 0;
    }
  </style>
</head>
<body>
  
  <!-- Botones de acción -->
  <div class="print-buttons">
    <button class="btn-print" onclick="window.print()">
      🖨️ Imprimir
    </button>
    <button class="btn-print btn-secondary" onclick="window.close()">
      ✕ Cerrar
    </button>
  </div>
  
  <!-- Etiqueta -->
  <div class="label-container">
    <div class="label-grid">
      
      <!-- Header: Logo + To Address -->
      <div class="header-section">
        <div class="logo-box">
          <div class="logo-text">
            T<br>A<br>B
          </div>
        </div>
        <div class="to-address-box">
          <div class="to-label">TO:</div>
          <div class="to-company"><%= envio.nombre_empresa || 'CLIENTE' %></div>
          <div class="to-details">
            <%= envio.destino || 'Dirección de destino' %>
            <% if (envio.contacto) { %><br><%= envio.contacto %><% } %>
            <% if (envio.telefono) { %><br>Tel: <%= envio.telefono %><% } %>
          </div>
        </div>
      </div>
      
      <!-- Priority Mail -->
      <div class="priority-section">
        <div class="priority-text">ENVÍO EXPRESO</div>
      </div>
      
      <!-- From Section -->
      <div class="from-section">
        <div class="from-label">FROM:</div>
        <div class="from-company">TRANSPORTES AB</div>
        <div class="from-details">
          <%= envio.origen || 'Origen' %><br>
          Tel: (81) 8888-8888<br>
          www.transportesab.com
        </div>
      </div>
      
      <!-- Order Info: Barcode + Details -->
      <div class="order-info-section">
        <div class="order-left">
          <div class="order-label">ORDER NR:</div>
          <div class="order-value"><%= envio.numero_tracking %></div>
          <div class="barcode-small">
            <canvas id="barcodeSmall"></canvas>
          </div>
        </div>
        <div class="order-right">
          <div class="info-field">
            <div class="order-label">LOT NUMBER:</div>
            <div class="order-value"><%= envio.id %></div>
          </div>
          <div class="info-field">
            <div class="order-label">SHIP DATE:</div>
            <div class="order-value"><%= new Date(envio.fecha_creacion).toLocaleDateString('es-MX', { day: '2-digit', month: '2-digit', year: 'numeric' }) %></div>
          </div>
        </div>
      </div>
      
      <!-- Item Info -->
      <div class="item-info-section">
        <div class="item-left">
          <div class="order-label">ITEM NR:</div>
          <div class="order-value"><%= envio.numero_tracking %></div>
        </div>
        <div class="item-right">
          <div class="order-label">WEIGHT:</div>
          <div class="order-value"><%= envio.peso ? envio.peso + ' KG' : 'N/A' %></div>
        </div>
      </div>
      
      <!-- Footer: Fragile + Arrows + Barcode -->
      <div class="footer-section">
        <div class="fragile-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 2L4 7v10l8 5 8-5V7l-8-5z"/>
            <path d="M12 12v5"/>
            <path d="M12 7h.01"/>
          </svg>
          <div class="fragile-text">FRAGILE</div>
        </div>
        
        <div class="arrows-icon">
          <div class="arrow">↑</div>
          <div class="arrow">↑</div>
          <div class="arrow-line"></div>
        </div>
        
        <div class="barcode-large">
          <canvas id="barcodeLarge"></canvas>
          <div class="barcode-number"><%= envio.numero_tracking %></div>
        </div>
      </div>
      
    </div>
  </div>
  
  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.6/dist/JsBarcode.all.min.js"></script>
  
  <script>
    window.addEventListener('load', function() {
      try {
        // Código de barras pequeño (ORDER NR)
        JsBarcode("#barcodeSmall", "<%= envio.numero_tracking %>", {
          format: "CODE128",
          width: 1.5,
          height: 40,
          displayValue: false,
          margin: 0,
          background: "transparent"
        });
        
        console.log('✅ Código de barras pequeño generado');
      } catch (error) {
        console.error('❌ Error al generar código de barras pequeño:', error);
      }
      
      try {
        // Código de barras grande (footer)
        JsBarcode("#barcodeLarge", "<%= envio.numero_tracking %>", {
          format: "CODE128",
          width: 1.5,
          height: 60,
          displayValue: false,
          margin: 0,
          background: "transparent"
        });
        
        console.log('✅ Código de barras grande generado');
      } catch (error) {
        console.error('❌ Error al generar código de barras grande:', error);
      }
    });
  </script>
</body>
</html>